# Claude Code local config (do not share)
.claude/

# Build artifacts
build/
*.o
*.out
hello
a.out

# CUDA / binaries
*.exe

# Datasets (generated — too large to commit)
*.bin

# Benchmark CSV output (generated at runtime)
results/*.csv

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# LaTeX report build artifacts (make report removes these; keep main.pdf)
report/*.aux
report/*.log
report/*.out
report/*.bbl
report/*.blg
report/*.toc
report/*.fls
report/*.fdb_latexmk
report/*.synctex.gz
